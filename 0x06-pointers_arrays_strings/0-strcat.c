#include "main.h"
#include <string.h>

/**
 * _strcat - Concatenates two strings
 * @dest: first string
 * @src: second string
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);

	return (0);
}
