#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcat - catenate two strings
 * @dest: first string
 * @src: second string
 *
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
