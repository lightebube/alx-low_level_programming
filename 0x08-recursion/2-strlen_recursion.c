#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - prints the length of a string
 * @s: string pointer
 * Return: length of string
 */
int _strlen_recursion(char *s)
{
	int count;

	count = strlen(s);

	return(count);
}
