#include <stdio.h>
/*
 * Main - Entry point
 * Return - Always 0 (Success)
 */

int main(void)
	/**
	 * main - Entry point
	 * Return: Always 0 (Success)
	 */
{
	const char *message = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", message);

	return (0);
}
